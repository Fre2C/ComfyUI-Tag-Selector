/* PLACEHOLDER — not a working PapaParse build.
 *
 * The plan is to vendor the real papaparse.min.js (v5.4.1, MIT,
 * https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js)
 * into this file and point tag_selector.html at ./papaparse.min.js so the
 * plugin works fully offline. Until then tag_selector.html keeps using the
 * CDN copy and simply shows a friendly toast when it is unreachable.
 */
